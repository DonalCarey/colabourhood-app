# Supabase setup

Project: `colabourhood`

Project URL:

```text
https://htooqkvrrkfcwknlodfd.supabase.co
```

## First setup

In Supabase, open **SQL Editor** and run these files in order:

1. `schema.sql`
2. `seed.sql`

Do not paste or commit the Supabase `service_role` key. The browser app should only use the anon public key.
